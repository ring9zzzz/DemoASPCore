@if (ViewData["TitleContent"] != null)
{
    <div class="content_top col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <div class="heading" style="display:flex;">
            <button class="glyphicon glyphicon-chevron-left back-button"></button>      
            <div>
                <h3>@ViewData["TitleContent"]</h3>
            </div>
        </div>
        <div class="clear"></div>
    </div>
}

